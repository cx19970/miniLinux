Packed onto the boot partition at the same relative paths.

    grub/grub.conf         real GRUB1 menu
    grub/menu.lst          GRUB1 compat: configfile /grub/grub.conf
    grub/grub.cfg          same as menu.lst (not GRUB2)
