本项目负责二进制及脚本集成，把程序和数据封装为 initrd、img 包，使用户基于二进制内容构建完整的可运行的 Linux 系统。

本项目使用的二进制程序主要基于 Buildroot 项目进行构建，计划开放编译配置及说明。

本项目应用程序构建尽量采用静态编译，使程序依赖库向 ld-linux、libc、vdso 收敛集中，以提高程序兼容性。

本项目依然会尽量提供通用的动态库，为了使从其它 Linux 发行版拿过来的程序可以直接运行。

本项目内不支持源码编译构建，不关注程序来源，为了便捷好用，放弃了部分安全性及可审核性。
